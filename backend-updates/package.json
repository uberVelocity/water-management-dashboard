{
  "name": "backend-updates",
  "version": "1.0.0",
  "description": "Consumes Kafka and live updates the frontend through a socket connection.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "kafkajs": "^1.14.0",
    "socket.io": "^2.3.0"
  }
}
